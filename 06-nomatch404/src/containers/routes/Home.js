import React from "react";

const Home = () => {
    return (
        <p>
            A
            <code> &lt;Switch&gt; </code>
            renders the first child
            <code> &lt;Route&gt; </code>
            that matches. A
            <code> &lt;Route&gt; </code>
            with no
            <code> path </code>
            always matches.
        </p>
    );
};

export default Home;
